# Leave empty to use static snapshot (/forecast_results.json) or mock fallback.
# Set to http://localhost:8000 (or leave unset and use Vite proxy: /api → :8000).
VITE_API_BASE_URL=

# Weeks requested from GET /api/forecast
VITE_FORECAST_HORIZON=12
