/**
 * TargetPlanning — hidden from the demo navigation.
 * This section discussed net-profit targets which are out of scope for the
 * Damm hackathon challenge (focus: sales / budget / promotions).
 * Kept in the codebase for reference; not rendered by App.tsx.
 */
export default function TargetPlanning() {
  return (
    <div className="flex items-center justify-center h-64 text-ink-400 text-[13px]">
      Target Planning is not available in this demo.
    </div>
  );
}
